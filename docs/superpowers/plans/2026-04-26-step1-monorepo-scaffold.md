# Step 1: Monorepo Scaffold + Prisma Schema + Neon Connection Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Create the pnpm workspace monorepo with three packages (`web`, `scraper`, `extension`), wire the full Prisma schema, and confirm the setup is valid without a real database connection.

**Architecture:** pnpm workspaces monorepo at repo root; Prisma lives exclusively in `web/` (scraper uses HTTP, never direct DB); `web/.env` carries a syntactically valid placeholder `DATABASE_URL` so `prisma validate` and `prisma generate` pass without a live Neon connection.

**Tech Stack:** pnpm 9.15, Next.js 15, React 19, TypeScript 5, Tailwind CSS v4 (CSS-first), shadcn/ui 2.x (CLI tool, not npm package), Prisma 6, Node 20+

---

## File Map

Files created in this step:

| File | Responsibility |
|------|---------------|
| `package.json` | Root workspace manifest + pnpm version pin |
| `pnpm-workspace.yaml` | Declares workspace packages |
| `.gitignore` | Single root gitignore covering all packages |
| `scraper/package.json` | scraper workspace identity + deps |
| `scraper/tsconfig.json` | scraper TypeScript config (commonjs) |
| `scraper/src/index.ts` | Minimal stub entry point |
| `extension/package.json` | extension workspace identity |
| `extension/manifest.json` | Chrome MV3 manifest |
| `web/package.json` | Next.js app deps + dev deps |
| `web/tsconfig.json` | TypeScript config with `@/*` alias |
| `web/next.config.ts` | Empty Next.js config |
| `web/postcss.config.mjs` | PostCSS wired to Tailwind v4 |
| `web/prisma/schema.prisma` | Full 6-model Prisma schema |
| `web/lib/prisma.ts` | Singleton PrismaClient (hot-reload safe) |
| `web/app/layout.tsx` | Root layout stub importing globals.css |
| `web/app/page.tsx` | Minimal home page stub |
| `web/.env` | Placeholder DATABASE_URL for Prisma CLI |

Files generated by tooling (do NOT create manually):

| File | Generated by |
|------|-------------|
| `web/lib/utils.ts` | `shadcn@2 init` |
| `web/components.json` | `shadcn@2 init` |
| `web/app/globals.css` | `shadcn@2 init` |
| `web/node_modules/.prisma/client/` | `prisma generate` |

---

## Chunk 1: Root Scaffold + scraper + extension Packages

### Task 1: Root package.json

**Files:**
- Create: `package.json`

- [ ] **Step 1: Create root `package.json`**

```json
{
  "name": "job-tracker",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm --filter web dev"
  }
}
```

Save to `job-tracker/package.json` (repo root).

- [ ] **Step 2: Verify JSON is valid**

```bash
node -e "require('./package.json'); console.log('ok')"
```

Expected: `ok`

---

### Task 2: pnpm workspace config

**Files:**
- Create: `pnpm-workspace.yaml`

- [ ] **Step 1: Create `pnpm-workspace.yaml`**

```yaml
packages:
  - "web"
  - "scraper"
  - "extension"
```

Save to `job-tracker/pnpm-workspace.yaml`.

---

### Task 3: Root .gitignore

**Files:**
- Create: `.gitignore`

- [ ] **Step 1: Create `.gitignore`**

```
node_modules/
.env
.env.local
.env*.local
dist/
.next/
*.tsbuildinfo
prisma/migrations/
```

Save to `job-tracker/.gitignore`.

Note: This single root file covers all packages. No per-package `.gitignore` files are needed.

---

### Task 4: scraper package

**Files:**
- Create: `scraper/package.json`
- Create: `scraper/tsconfig.json`
- Create: `scraper/src/index.ts`

- [ ] **Step 1: Create `scraper/package.json`**

```json
{
  "name": "scraper",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "ts-node src/index.ts"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
```

- [ ] **Step 2: Create `scraper/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "es2022",
    "module": "commonjs",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

- [ ] **Step 3: Create `scraper/src/index.ts`**

```ts
console.log("scraper ready");
```

---

### Task 5: extension package

**Files:**
- Create: `extension/package.json`
- Create: `extension/manifest.json`

- [ ] **Step 1: Create `extension/package.json`**

```json
{
  "name": "extension",
  "version": "0.0.1",
  "private": true
}
```

- [ ] **Step 2: Create `extension/manifest.json`**

```json
{
  "manifest_version": 3,
  "name": "Job Tracker",
  "version": "0.0.1",
  "description": "Auto-marks job applications as applied",
  "permissions": [],
  "host_permissions": []
}
```

- [ ] **Step 3: Commit Chunk 1**

```bash
git add package.json pnpm-workspace.yaml .gitignore scraper/ extension/
git commit -m "feat: add root workspace config and scraper/extension stubs"
```

---

## Chunk 2: web/ Package Files

### Task 6: web/package.json

**Files:**
- Create: `web/package.json`

- [ ] **Step 1: Create `web/package.json`**

```json
{
  "name": "web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "tailwindcss": "^4.0.0",
    "@tailwindcss/postcss": "^4.0.0"
  }
}
```

Note: shadcn is NOT listed here — it is a CLI scaffolding tool invoked via `pnpm dlx`, not an npm dependency.

---

### Task 7: web/tsconfig.json

**Files:**
- Create: `web/tsconfig.json`

- [ ] **Step 1: Create `web/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": { "@/*": ["./*"] }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Note: The `@/*` path alias resolves relative to `web/`. It is scoped to `web/` only — code in `scraper/` or `extension/` cannot use it.

---

### Task 8: web/next.config.ts and web/postcss.config.mjs

**Files:**
- Create: `web/next.config.ts`
- Create: `web/postcss.config.mjs`

- [ ] **Step 1: Create `web/next.config.ts`**

```ts
import type { NextConfig } from "next";
const nextConfig: NextConfig = {};
export default nextConfig;
```

- [ ] **Step 2: Create `web/postcss.config.mjs`**

```js
export default {
  plugins: { "@tailwindcss/postcss": {} },
};
```

Note: No `tailwind.config.ts` is created. Tailwind v4 is CSS-first — all configuration happens in `globals.css` via `@import "tailwindcss"` and `@theme {}`. That file is generated by shadcn init in Task 12.

---

### Task 9: web/prisma/schema.prisma

**Files:**
- Create: `web/prisma/schema.prisma`

- [ ] **Step 1: Create `web/prisma/schema.prisma`**

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Job {
  id          String    @id @default(cuid())
  title       String
  company     String
  url         String    @unique
  source      String
  location    String
  description String?
  postedAt    DateTime
  foundAt     DateTime  @default(now())
  snapshotUrl String?
  userJobs    UserJob[]
}

model UserJob {
  id          String    @id @default(cuid())
  userId      String
  jobId       String
  score       Int?
  scoreReason String?
  status      String    @default("new")
  appliedAt   DateTime?
  emailNote   String?
  user        User      @relation(fields: [userId], references: [id])
  job         Job       @relation(fields: [jobId], references: [id])

  @@unique([userId, jobId])
}

model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  image         String?
  gmailToken    String?
  aiProvider    String?
  aiApiKey      String?
  resumeUrl     String?
  resumeText    String?
  resumeParsed  Json?
  skillsProfile String?
  apiKey        String    @unique @default(cuid())
  userJobs      UserJob[]
  accounts      Account[]
  sessions      Session[]
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}
```

---

### Task 10: web/lib/prisma.ts

**Files:**
- Create: `web/lib/prisma.ts`

- [ ] **Step 1: Create `web/lib/prisma.ts`**

```ts
import { PrismaClient } from "@prisma/client";

const globalForPrisma = global as unknown as { prisma: PrismaClient };
export const prisma = globalForPrisma.prisma ?? new PrismaClient();
if (process.env.NODE_ENV !== "production") globalForPrisma.prisma = prisma;
```

This singleton pattern prevents connection pool exhaustion from Next.js hot-reload creating multiple `PrismaClient` instances in development.

---

### Task 11: web/app stubs

**Files:**
- Create: `web/app/layout.tsx`
- Create: `web/app/page.tsx`

- [ ] **Step 1: Create `web/app/layout.tsx`**

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = { title: "Job Tracker" };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

Note: `globals.css` is imported here but does NOT exist yet — it is generated by `shadcn@2 init` in Task 12. This ordering is intentional.

- [ ] **Step 2: Create `web/app/page.tsx`**

```tsx
export default function Home() {
  return <main><h1>Job Tracker</h1></main>;
}
```

---

### Task 12: web/.env placeholder

**Files:**
- Create: `web/.env`

- [ ] **Step 1: Create `web/.env`**

```env
DATABASE_URL="postgresql://placeholder:placeholder@localhost:5432/placeholder"
```

This file is excluded from git by the root `.gitignore` (`.env` pattern). Replace the placeholder value with the real Neon pooled connection string (including `?sslmode=require`) before running `prisma migrate dev`.

Why `web/.env` and not `web/.env.local`: Prisma CLI loads `web/.env` automatically via dotenv. `web/.env.local` is a Next.js-only convention — Prisma CLI does not load it. Using `web/.env` ensures `prisma validate` and `prisma generate` work without extra flags.

- [ ] **Step 2: Commit Chunk 2**

```bash
git add web/
git commit -m "feat: add web package files, Prisma schema, and lib/prisma.ts"
```

---

## Chunk 3: Install, shadcn Init, and Verification

### Task 13: Install dependencies

**Files:** none (installs to node_modules)

- [ ] **Step 1: Run `pnpm install` from repo root**

```bash
# from job-tracker/ (repo root)
pnpm install
```

Expected: exits 0. All three workspace packages resolved. No peer dependency errors. `node_modules/` created at repo root and inside each workspace.

If you see peer dependency warnings (not errors), that is acceptable. Errors that prevent install must be resolved before proceeding.

---

### Task 14: shadcn/ui init

**Files generated (do NOT create these manually before this step):**
- `web/lib/utils.ts` — exports `cn()` helper
- `web/components.json` — shadcn config file
- `web/app/globals.css` — `@import "tailwindcss"` + shadcn CSS variable definitions

- [ ] **Step 1: Run shadcn init from `web/`**

```bash
cd web && pnpm dlx shadcn@2 init --defaults
```

The `--defaults` flag skips all interactive prompts. This sets style=New York, baseColor=neutral, CSS variables=yes.

Expected: exits 0. Three files appear: `lib/utils.ts`, `components.json`, `app/globals.css`.

Note: `tailwind.config.ts` is NOT generated with Tailwind v4 + shadcn 2.x. If the CLI version you get generates one anyway, keep it — do not delete it. But do not create it manually if it is absent.

- [ ] **Step 2: Verify generated files exist**

```bash
# from web/
ls lib/utils.ts components.json app/globals.css
```

Expected: all three files listed without error.

- [ ] **Step 3: Commit generated files**

```bash
# from repo root
git add web/lib/utils.ts web/components.json web/app/globals.css
git commit -m "feat: run shadcn init — add globals.css, utils.ts, components.json"
```

---

### Task 15: Prisma schema validation

**Files:** none (read-only check)

- [ ] **Step 1: Run `prisma validate` from `web/`**

```bash
cd web && pnpm prisma validate
```

Expected output contains: `The schema at prisma/schema.prisma is valid 🚀`

If it fails with a connection error, the `DATABASE_URL` placeholder is not being picked up. Confirm `web/.env` exists and contains the placeholder value from Task 12.

---

### Task 16: Prisma client generation

**Files generated:**
- `web/node_modules/.prisma/client/` (default output location — no custom `output` in schema)

- [ ] **Step 1: Run `prisma generate` from `web/`**

```bash
cd web && pnpm prisma generate
```

Expected: exits 0. Output includes a line referencing the generated client path.

- [ ] **Step 2: Confirm client is importable**

```bash
# from web/
node --input-type=commonjs -e "require('@prisma/client'); console.log('prisma client ok')"
```

Expected output: `prisma client ok`

If this fails with `Cannot find module '@prisma/client'`, the generate step did not complete. Re-run `pnpm prisma generate` and check for errors.

- [ ] **Step 3: Final commit**

```bash
# from repo root
git add -A
git commit -m "feat: complete Step 1 — monorepo scaffold, Prisma schema, client generated"
```

---

## Verification Summary

All three checks must pass before Step 1 is complete:

| Check | Command | Expected |
|-------|---------|----------|
| Install | `pnpm install` (from root) | exits 0, no errors |
| Schema valid | `cd web && pnpm prisma validate` | "schema is valid 🚀" |
| Client generated | `node --input-type=commonjs -e "require('@prisma/client'); console.log('prisma client ok')"` | `prisma client ok` |

## What This Step Does NOT Include

- No NextAuth.js wiring (Step 2)
- No API routes (Steps 4+)
- No actual database migration (requires real `DATABASE_URL`)
- No scraper logic (Step 5)
- No UI beyond `app/layout.tsx` and `app/page.tsx`
- No shadcn components added (added on demand in later steps via `pnpm dlx shadcn@2 add`)
